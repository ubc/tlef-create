# Staging Server Environment Configuration
# This file shows the exact environment variables used on staging server

NODE_ENV=production
PORT=8051
SAML_AVAILABLE=false

# Database - Note: Uses MONGO_URI not MONGODB_URI
MONGO_URI=mongodb://create_staging_user:PW_HERE@10.93.3.100:27017/create_staging
DB_NAME=create_staging

# Vector Database
QDRANT_URL=http://10.93.3.108:6333
QDRANT_API_KEY=PW_HERE

# Session
SESSION_SECRET=abcdefg

# URLs - Note: These have ports but will be stripped in code
FRONTEND_URL=https://create.staging.apps.ltic.ubc.ca:8092
VITE_API_URL=https://create.staging.apps.ltic.ubc.ca:8051

# File Upload
UPLOAD_DIR=./routes/create/uploads
MAX_FILE_SIZE=104857600

# AI Configuration
EMBEDDING_PROVIDER=ubc-genai-toolkit-llm
LLM_PROVIDER=ollama
LLM_API_KEY=nokey
LLM_ENDPOINT=http://localhost:11434
LLM_EMBEDDING_MODEL=nomic-embed-text

# Redis - Only password is provided
REDIS_PASSWORD=PW_HERE
# If Redis host/port are different from default, add:
# REDIS_HOST=localhost
# REDIS_PORT=6379