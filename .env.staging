NODE_ENV=production
PORT=8051
SAML_AVAILABLE=false

# Database Configuration
MONGODB_URI=mongodb://create_staging_user:PW_HERE@10.93.3.100:27017/create_staging
DB_NAME=create_staging

# Vector Database
QDRANT_URL=http://10.93.3.108:6333
QDRANT_API_KEY=PW_HERE

# Redis
REDIS_PASSWORD=PW_HERE

# Session
SESSION_SECRET=abcdefg

# URLs - No port numbers for HTTPS URLs behind reverse proxy
FRONTEND_URL=https://create.staging.apps.ltic.ubc.ca
VITE_API_URL=https://create.staging.apps.ltic.ubc.ca

# File Upload
UPLOAD_DIR=./routes/create/uploads
MAX_FILE_SIZE=104857600

# AI Configuration
EMBEDDING_PROVIDER=ubc-genai-toolkit-llm
LLM_PROVIDER=ollama
LLM_API_KEY=nokey
LLM_ENDPOINT=http://localhost:11434
LLM_EMBEDDING_MODEL=nomic-embed-text

# Use the lighter server.js from src/ directory for staging
TLEF_CREATE_PORT=8090